{
  "buildCommand": "cd vercel-ui && npm install && npm run build",
  "outputDirectory": "vercel-ui/.next",
  "installCommand": "cd vercel-ui && npm install",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/vercel-ui/$1"
    }
  ]
}
